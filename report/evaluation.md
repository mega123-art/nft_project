# Phase 8 evaluation report

Generated from `/home/parth/nft_project/logs/decisions.db` by `scripts/evaluate.py`.

## False-safe rate (most important number in this report)

**NOT COMPUTED** -- no ground truth at /home/parth/nft_project/data/ground_truth/frame_labels.csv (see data/ground_truth/SCHEMA.md).

To unblock: annotate `data/ground_truth/frame_labels.csv` (schema in `data/ground_truth/SCHEMA.md`), e.g. via `python scripts/annotate_ground_truth.py <video> --video-source <name>` while `--log-db` was pointed at the same DB this report reads.

## End-to-end latency (frame capture -> audio start)

n = 377 logged frames

| stat | ms |
|---|---|
| mean | 33.9 |
| p50 | 32.3 |
| p95 | 35.9 |
| max | 550.2 |

Target: p95 < 300 ms -- **MEETS** target.

## Frame rate

| video_source | frames | fps |
|---|---|---|
| data/raw_videos/car-detection.mp4 | 377 | 30.37 |

Overall: 30.37 fps across all logged frames.

## TTC error vs. manually annotated ground truth

**NOT COMPUTED** -- no ground truth at /home/parth/nft_project/data/ground_truth/ttc_observations.csv (see data/ground_truth/SCHEMA.md).

To unblock: annotate `data/ground_truth/ttc_observations.csv` (schema in `data/ground_truth/SCHEMA.md`) against ~20 vehicle tracks, matched by (video_source, frame_index) to rows already in decisions.db.

## Per-class mAP50 comparison (Phase 2 vs Phase 3 sub vs Phase 3 full)

This is PLAN.md's "strongest graph in the report" -- it is a table here; plot it directly from these numbers for the write-up.

| class | Phase 2 (public data only) | Phase 3 sub (+signals, capped 1.5k) | Phase 3 full (+signals, all 8.1k) |
|---|---|---|---|
| car | 0.936 | 0.933 | 0.955 |
| truck | 0.883 | 0.869 | 0.848 |
| motorcycle | 0.882 | 0.870 | 0.851 |
| autorickshaw | 0.896 | 0.913 | 0.888 |
| person | 0.576 | 0.690 | 0.710 |
| crosswalk | 0.825 | 0.874 | 0.872 |
| bus | -- | 0.631 | 0.718 |
| signal_red | -- | 0.648 | 0.594 |
| signal_green | -- | 0.600 | 0.675 |

## Confusion matrix and PR curves

Not regenerated here. Ultralytics writes these automatically under `runs/detect/<run_name>/{confusion_matrix.png,PR_curve.png,...}` as a side effect of `scripts/train.py` (or a plain `model.val()` call) -- but the training pod that produced Phase 2/3's `runs/` directory has been destroyed, and that directory is gitignored, so those artifacts do not exist anywhere in this repo right now. They need a fresh training or validation run (on any machine with the dataset and `models/best.pt`) to regenerate; running `model.val(data="data/data.yaml")` alone is enough, a full retrain is not required.

## Annotated screenshots

Not generated by this script. `report/phase6_fsm_check/` and `report/phase2_best_check/` already contain a handful of annotated frames from earlier phases (state + reason drawn, per Phase 6's own done-check) -- reuse `main.py --save-frames DIR` against clips that hit each of SEARCHING / WAITING / SAFE_TO_CROSS to get the 6-8 screenshots PLAN.md asks for; none of the current sample clips are confirmed to reach SAFE_TO_CROSS (see the ground-truth note above on why -- sparse detections on non-Indian CCTV footage).
